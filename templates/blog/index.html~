{% extends 'base.html' %}

{% block section %}
<table class="table">
  <tr>
    {% for key in items[0].keys() %}
    <th>{{ key }}</th>
    {% endfor %}
  </tr>
  {% for item in items %}
    {% for value in items[0].values() %}
    <td>{{ value }}</td>
    {% endfor %}
  <tr>
</table>
{% endblock %}
